My fisrt HELM chart

You can connect to MySQL with
POD=$(kubectl get pods -n default -l app=mysql -o jsonpath="{.items[0].metadata.name}")
adminPass=$(echo 'c2VjcmV0cGFzc3dvcmQ=' | base64 --decode)
kubectl exec $POD -it -- mysql -u{{ .Values.dbUser }} -p $adminPass
